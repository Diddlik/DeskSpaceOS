<Page
    x:Class="DeskSpaceOS_SettingsApp.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Spacing="24" Padding="0,0,16,24">
            <TextBlock Text="About" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="Information about the application."
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="24">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>

                    <Image Grid.Column="0" Source="Assets/StoreLogo.png" Width="96" Height="96" VerticalAlignment="Top" Margin="0,0,24,0" />

                    <StackPanel Grid.Column="1" Spacing="8">
                        <TextBlock Text="DeskSpace OS" FontSize="20" FontWeight="SemiBold" />
                        <TextBlock Text="{x:Bind AppVersion}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                        
                        <TextBlock Text="A modern, open-source desktop organizer that groups your shortcuts and files into highly customizable visual spaces (spaces). Clean up your digital workspace and boost your productivity."
                                   TextWrapping="Wrap" Margin="0,8,0,16" />

                        <TextBlock Text="Credits &amp; Open Source Packages" FontWeight="SemiBold" FontSize="14" Margin="0,8,0,4" />
                        <TextBlock Text="Built with passion for the desktop community. This application uses the following open-source libraries and technologies:"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,4" />
                        
                        <RichTextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" TextWrapping="Wrap">
                            <Paragraph>• <Run FontWeight="SemiBold">Microsoft.WindowsAppSDK</Run> (WinUI 3)</Paragraph>
                            <Paragraph>• <Run FontWeight="SemiBold">Microsoft.Extensions.Hosting</Run> &amp; <Run FontWeight="SemiBold">Logging</Run></Paragraph>
                            <Paragraph>• <Run FontWeight="SemiBold">Win32 APIs</Run> (SysListView32 hook &amp; user32.dll integrations)</Paragraph>
                            <Paragraph>Special thanks to the contributors and the C# community.</Paragraph>
                        </RichTextBlock>
                    </StackPanel>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>