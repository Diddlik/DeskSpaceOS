<Page
    x:Class="DeskSpaceOS_SettingsApp.HotkeysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded">

    <ScrollViewer>
        <StackPanel Spacing="24" Padding="0,0,16,24">
            <TextBlock Text="Hotkeys" FontSize="24" FontWeight="SemiBold" />
            <TextBlock Text="Configure keyboard shortcuts for quick actions."
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />

            <!-- Peek Mode -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16,12">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="2">
                        <TextBlock Text="Peek Mode" FontWeight="SemiBold" />
                        <TextBlock Text="Bring all spaces above active windows"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                    </StackPanel>
                    <TextBox x:Name="PeekHotkeyBox" Grid.Column="1" Width="160"
                             PlaceholderText="e.g. Win+Space" />
                </Grid>
            </Border>

            <!-- Distraction Free -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16,12">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="2">
                        <TextBlock Text="Distraction Free Mode" FontWeight="SemiBold" />
                        <TextBlock Text="Hide all spaces and icons"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                    </StackPanel>
                    <TextBox x:Name="DistractionFreeHotkeyBox" Grid.Column="1" Width="160"
                             PlaceholderText="e.g. Win+D" />
                </Grid>
            </Border>

            <!-- New Space -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16,12">
                <Grid ColumnSpacing="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="2">
                        <TextBlock Text="New Space" FontWeight="SemiBold" />
                        <TextBlock Text="Start drawing a new space"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                    </StackPanel>
                    <TextBox x:Name="NewSpaceHotkeyBox" Grid.Column="1" Width="160"
                             PlaceholderText="e.g. Ctrl+Shift+N" />
                </Grid>
            </Border>

            <Button Content="Save Hotkeys" Style="{ThemeResource AccentButtonStyle}" Click="SaveButton_Click" />
            <InfoBar x:Name="StatusInfoBar" IsOpen="False" IsClosable="True" />
        </StackPanel>
    </ScrollViewer>
</Page>
